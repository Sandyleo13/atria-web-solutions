import { Star } from "lucide-react";
import { Container } from "@/components/layout/Container";
import { SectionTitle } from "@/components/common/SectionTitle";
import { testimonials } from "@/data/home";

export function TestimonialsSection() { return <section className="border-y border-line bg-surface py-24 sm:py-28"><Container><SectionTitle align="center" eyebrow="Clients love us" title="What Our Clients Say" /><div className="mt-14 grid gap-5 sm:grid-cols-2 xl:grid-cols-4">{testimonials.map((testimonial) => <figure className="rounded-2xl border border-line bg-white p-6 shadow-card transition duration-300 hover:-translate-y-1 hover:shadow-card-hover" key={testimonial.name}><div className="flex gap-1 text-brand">{Array.from({ length: 5 }).map((_, index) => <Star key={index} size={14} fill="currentColor" />)}</div><blockquote className="mt-4 text-sm leading-6 text-muted">&ldquo;{testimonial.quote}&rdquo;</blockquote><figcaption className="mt-6 flex items-center gap-3"><span className="grid size-9 place-items-center rounded-full bg-brand-soft text-xs font-bold text-brand-dark">{testimonial.initials}</span><div><p className="text-xs font-bold">{testimonial.name}</p><p className="text-[11px] text-muted">{testimonial.role}</p></div></figcaption></figure>)}</div><div className="mt-8 flex justify-center gap-1.5">{Array.from({ length: 5 }).map((_, index) => <span key={index} className={`size-1.5 rounded-full transition-transform duration-300 hover:scale-150 ${index === 0 ? "bg-brand" : "bg-slate-300"}`} />)}</div></Container></section>; }
