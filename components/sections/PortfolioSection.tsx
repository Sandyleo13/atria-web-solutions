import { ArrowUpRight } from "lucide-react";
import Link from "next/link";
import { Container } from "@/components/layout/Container";
import { projects } from "@/data/home";

export function PortfolioSection() { return <section id="portfolio" className="scroll-mt-20 bg-white pb-24 sm:pb-28"><Container><div className="flex items-end justify-between gap-6"><div><p className="text-[11px] font-bold tracking-wide text-brand uppercase">Our portfolio</p><h2 className="mt-2 text-3xl font-bold tracking-[-0.045em]">Featured Projects</h2></div><Link href="#portfolio" className="hidden items-center gap-1 text-sm font-bold text-brand sm:inline-flex">View All Projects <ArrowUpRight size={16} /></Link></div><div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6">{projects.map((project) => <article key={project.name} className="group cursor-pointer"><div className={`relative aspect-[4/3] overflow-hidden rounded-xl bg-gradient-to-br ${project.accent} p-4 shadow-sm transition duration-300 group-hover:-translate-y-1 group-hover:shadow-card-hover`}><div className="absolute inset-4 rounded border border-white/20 transition-transform duration-500 group-hover:scale-105" /><div className="relative rounded-lg bg-white/10 p-3 text-xs font-semibold text-white backdrop-blur-sm">{project.name}</div></div><div className="mt-4 flex items-start justify-between gap-2"><div><h3 className="text-sm font-bold">{project.name}</h3><p className="mt-1 text-xs leading-4 text-muted">{project.category}</p></div><span className="grid size-7 shrink-0 place-items-center rounded-full bg-brand-soft text-brand transition-transform duration-300 group-hover:-translate-y-0.5 group-hover:translate-x-0.5"><ArrowUpRight size={14} /></span></div></article>)}</div></Container></section>; }
